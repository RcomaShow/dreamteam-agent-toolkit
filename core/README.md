# DreamTeam Core

The core defines concepts that adapters must preserve:

- task classification;
- token-aware routing;
- worker contracts;
- compact evidence handoffs;
- escalation;
- quality gates;
- task ledgers;
- budget profiles.

Core documents must not depend on a specific model, vendor, tool name, or agent file format.
