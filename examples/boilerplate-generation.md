# Scaffolding Example

```text
ROUTE|WORKER_WRITE
CLASS|M0
WORKER|execution-scaffold-builder
```

The orchestrator supplies a blueprint with exact files, signatures, behavior, reserved decisions, and verification. The worker creates only the mechanical structures and returns CHP/2 changes and handoffs.
