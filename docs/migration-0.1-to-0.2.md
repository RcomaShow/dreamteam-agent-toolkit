# Migration from 0.1 to 0.2

- `DCP/1` and `CHP/1` are replaced by parsable `DCP/2` and `CHP/2`.
- `repo-scout` is replaced by locator, tracer, pattern, and impact workers.
- `structure-builder` is replaced by scaffold and bounded-logic workers; mechanical editor remains specialized.
- Add `/dreamteam:doctor`.
- Add `profile=offload` to prioritize main-model conservation.
- Existing `/dreamteam:run`, `/dreamteam:review`, and `/dreamteam:measure` remain.
- No Agent Team behavior is enabled.
